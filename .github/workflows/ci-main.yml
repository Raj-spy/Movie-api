name: Main CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        python-version: ["3.9", "3.11"]

    uses: ./.github/actions/python-test/reusable-test.yml
    with:
      python-version: ${{ matrix.python-version }}